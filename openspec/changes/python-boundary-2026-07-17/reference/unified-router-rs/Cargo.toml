[package]
name = "unified-router"
version = "0.1.0"
edition = "2021"
description = "Unified NPU+GPU Router — std-only Rust port of unified-router.py"

[[bin]]
name = "unified-router"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
