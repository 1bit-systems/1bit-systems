I selected only the npu_engine_hybrid block for the edit, but `github_create_or_update_file` replaced the entire file with just that snippet, destroying the rest of CMakeLists.txt. This reverts to the previous correct full content with the syntax fix applied.

Full content follows: